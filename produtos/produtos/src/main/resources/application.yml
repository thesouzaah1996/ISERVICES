spring:
  application:
    name: produtos
  datasource:
    url: jdbc:postgresql://localhost:5555/icomprasprodutos
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-plataform: org.hibernate.dialect.PostgresSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081